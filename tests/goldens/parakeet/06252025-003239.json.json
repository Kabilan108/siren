{"text":"Alright, so I have spent some time, I've implemented the spectrogram approach into my code base, and it works. So right now I'm computing spectrograms for the ECG data, and then I'm essentially just training the models. I also set up some data augmentations that I can use during the training process. I've attached the current version of a lot of the code, and now I kind of wanted to just get your input on some of the issues I've noticed so far with the results I'm looking at. So the blue line is so both of these lines are for the shallow fine-tuned ResNet. The blue line is no augmentations, no transforms. The teal line is using frequency masking and time masking. You can see the parameters in the config file for both the normal and artifact class. Neither of these runs have oversampling or any kind of weighted loss. You can see we do have the models are learning and the loss keeps going down quite a bit. Accuracy on the train set goes up, but I don't think we have any overfitting issues. The accuracy is much less on the training set, on the sorry, the run with the augmentations. The accuracy on the training set is much less, but I think that's good because it means it's not like there's less of a risk of overfitting there. When you look at the other metrics, accuracy, like by the time the augmented run finishes, it's more or less like similar place on validation accuracy. The no augmentations does end up getting to a higher validation accuracy. What really improves significantly here is the specificity. We pretty much saturated on the validation set at 98%. Sensitivity is kind of taking a beating though. It's at 66% on the validation set. And then when you look at the final test set, the regular run gets like 79% sensitivity and only 30% sensitivity on the test set while getting 99% specificity on the test set. That's for the augmented run. The regular run gets 94% specificity and like I said, 79%. So we're getting somewhere, but I don't know. The augmentations seem to be harming my sensitivity and sensitivity is kind of the thing I care most about for these models. So what do you think? Any advice? I definitely think I should train the models for longer because it does seem like the loss is still going down. So I'm doing another run now that has that change implemented. Let me know what you think."}